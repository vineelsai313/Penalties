%===========================

holds(at_loc(9), 0).

%===========================

%===========================

goal(I) :- holds(at_loc(8), I), step(I).

%===========================

% Input:
non_emergency.
