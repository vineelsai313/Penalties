%===========================

holds(at_loc(6), 0).
holds(there_are_pedestrians(5), 1).

%===========================

%===========================

goal(I) :- holds(at_loc(4), I), step(I).

%===========================

% Input:
emergency.
