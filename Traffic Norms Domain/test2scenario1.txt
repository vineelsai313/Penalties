%===========================

holds(at_loc(6), 0).
holds(is_school_bus(13, 14), 3).

%===========================

%===========================

goal(I) :- holds(at_loc(10), I), step(I).

%===========================

% Input:
non_emergency.
