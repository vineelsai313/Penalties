%===========================

holds(at_loc(9), 0).

%===========================

%===========================

goal(I) :- holds(at_loc(12), I), step(I).

%===========================

% Input:
non_emergency.
