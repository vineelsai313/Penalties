%===========================

holds(at_loc(4), 0).
holds(light_color(red, 3, 1), 1).

%===========================

%===========================

goal(I) :- holds(at_loc(2), I), step(I).

%===========================

% Input:
emergency.
